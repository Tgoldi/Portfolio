# Skip optional dependencies (helps with platform-specific modules)
optional=false

# Use legacy peer dependencies resolution to avoid conflicts
legacy-peer-deps=true

# Allow scripts for build process
ignore-scripts=false

# Enable error-level logging for debugging
loglevel=error

# Force resolution of dependencies
public-hoist-pattern[]=*rollup*
public-hoist-pattern[]=*swc* 